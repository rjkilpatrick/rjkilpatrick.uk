---
const { src, alt, caption } = Astro.props;
---

<figure class="figure">
  <img src="{src}" alt="{alt}" />
  <figcaption>
    {caption}
  </figcaption>
  {caption ? <figcaption>{caption}</figcaption> : null}
</figure>
